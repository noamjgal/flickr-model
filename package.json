{
  "name": "flickr-visquill",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "preprocess": "python scripts/preprocess_data.py",
    "dev": "vite",
    "build": "npm run preprocess && tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@visquill/visquill-gdk": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
